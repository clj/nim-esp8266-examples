* New examples
  * Read data from flash
  * Connect to WiFi (with settings in flash)
* Change the following examples to read settings from flash:
  * mqtt-blinky
  * fota
* Build all examples for 1MB flash
* Fix travis build to no longer include dummy crendentials as
  these are now read from flash
